# ============================================
# BEMAT Production Configuration
# ============================================
# This file is used automatically in PRODUCTION mode

# ============================================
# BACKEND CONFIGURATION
# ============================================

# Django Settings
DEBUG=False
# IMPORTANT: Generate a new SECRET_KEY for production!
# Run: python -c 'import secrets; print(secrets.token_hex(50))'
SECRET_KEY=your-super-secret-production-key-here-generate-with-secrets-token-hex-50

# Allowed Hosts (comma-separated, no protocol)
ALLOWED_HOSTS=localhost,127.0.0.1,energymanagement.epu.ntua.gr

# CSRF Trusted Origins (comma-separated, WITH protocol)
CSRF_TRUSTED_ORIGINS=https://energymanagement.epu.ntua.gr

# CORS Allowed Origins (comma-separated, WITH protocol)
CORS_ALLOWED_ORIGINS=https://energymanagement.epu.ntua.gr

# Database Configuration (PostgreSQL for production)
SQL_ENGINE=django.db.backends.postgresql
SQL_DATABASE=backend_dev
SQL_USER=backend
# IMPORTANT: Use a strong password for production!
SQL_PASSWORD=backend
SQL_HOST=db
SQL_PORT=5432

# Email Configuration
EMAIL_HOST_USER=ioannis.lyras98@gmail.com
EMAIL_HOST_PASSWORD=optf etzu yswt esze
DEFAULT_FROM_EMAIL=ioannis.lyras98@gmail.com

# ============================================
# FRONTEND CONFIGURATION
# ============================================

# Backend API Base URL
VITE_API_BASE_URL=https://energymanagement.epu.ntua.gr/api

# Vite Development Server (not used in production, but here for completeness)
VITE_ALLOWED_HOSTS=energymanagement.epu.ntua.gr

# Enable file watching (not used in production nginx build)
CHOKIDAR_USEPOLLING=false
